export const codeExamples = "\n\n## 코드 하이라이팅 둘러보기\n\n각 코드 블록에 커서를 놓으면 툴바에서 언어를 변경할 수 있습니다. 오른쪽 문서 개요로 예제 사이를 이동해 보세요.\n\n### TypeScript · 타입과 비동기 함수\n\n제네릭, 키워드, 문자열 보간, 주석을 비교해 보세요.\n\n```typescript\n// 공개된 문서만 반환합니다.\ntype Artifact = { id: string; title: string; published: boolean };\n\nasync function findArtifacts<T extends Artifact>(items: T[]): Promise<T[]> {\n  const visible = items.filter(({ published }) => published);\n  console.info(`공개 문서: ${visible.length}개`);\n  return visible;\n}\n```\n\n### TSX · 컴포넌트와 JSX\n\n태그, 속성, JavaScript 표현식의 구분을 확인해 보세요.\n\n```tsx\ntype CardProps = { title: string; count: number };\n\nexport function ArtifactCard({ title, count }: CardProps) {\n  return (\n    <article aria-label={title}>\n      <h2>{title}</h2>\n      <span className=\"count\">{count > 0 ? `${count}개의 코멘트` : '새 문서'}</span>\n    </article>\n  );\n}\n```\n\n### Java · 어노테이션과 레코드\n\n어노테이션, 접근 제어자, 타입, 메서드 호출을 확인해 보세요.\n\n```java\n// 요청 본문 검증 예제\npublic record CreateArtifact(@NotBlank String title, String body) {}\n\n@PostMapping(\"/artifacts\")\npublic ResponseEntity<Artifact> create(@Valid @RequestBody CreateArtifact input) {\n    var artifact = service.create(input);\n    return ResponseEntity.status(201).body(artifact);\n}\n```\n\n### Python · 함수와 문자열\n\n독스트링, 타입 힌트, 숫자, 리스트 컴프리헨션을 비교해 보세요.\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass\nclass Note:\n    title: str\n    resolved: bool = False\n\ndef summarize(notes: list[Note]) -> str:\n    \"\"\"아직 정리하지 않은 메모 수를 계산합니다.\"\"\"\n    pending = [note for note in notes if not note.resolved]\n    return f\"남은 메모: {len(pending):02d}개\"\n\n```\n\n### SQL · 조회와 집계\n\n키워드, 함수, 별칭, 조건절을 확인해 보세요.\n\n```sql\n-- 최근 공개 문서가 많은 프로젝트\nSELECT project_id, COUNT(*) AS artifact_count\nFROM artifacts\nWHERE visibility = 'public'\n  AND created_at >= CURRENT_DATE - INTERVAL '7 days'\nGROUP BY project_id\nHAVING COUNT(*) > 3\nORDER BY artifact_count DESC\nLIMIT 10;\n```\n\n### JSON · API 응답\n\n키, 문자열, 숫자, 불리언, null을 비교해 보세요.\n\n```json\n{\n  \"id\": \"sample-artifact\",\n  \"title\": \"문서 편집 경험\",\n  \"version\": 3,\n  \"published\": true,\n  \"archivedAt\": null,\n  \"tags\": [\"요구사항\", \"에디터\"],\n  \"metadata\": { \"width\": 1280, \"height\": 720 }\n}\n```\n\n### CSS · 선택자와 테마 변수\n\n선택자, 속성, 단위, 함수와 미디어 쿼리를 확인해 보세요.\n\n```css\n.document-card:hover {\n  color: var(--color-text-accent);\n  padding: clamp(1rem, 3vw, 2rem);\n  border: 1px solid color-mix(in oklab, currentColor 20%, transparent);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .document-card { transition: none; }\n}\n```\n\n### Bash · 명령과 변수\n\n변수 확장, 따옴표, 조건문을 비교해 보세요. 실행되지 않는 예제입니다.\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\nPROJECT=\"Gentask\"\nif [ -n \"$PROJECT\" ]; then\n  printf '프로젝트: %s\\n' \"$PROJECT\"\nfi\n\n# 예제 파일 목록 출력\nfor file in docs/*.md; do\n  printf '%s\\n' \"$file\"\ndone\n```\n\n### YAML · 설정과 여러 줄 문자열\n\n중첩 키, 목록, 불리언, 여러 줄 문자열을 확인해 보세요.\n\n```yaml\neditor:\n  name: Gentask\n  enabled: true\n  languages:\n    - typescript\n    - java\n  description: |\n    문서를 작성하고\n    맥락을 함께 남깁니다.\n  limits:\n    maxImages: 10\n```\n\n### 일반 텍스트 · 강조 없음\n\n언어를 일반 텍스트로 바꾸면 구문 색상 없이 표시됩니다.\n\n```text\n2026-09-10 09:00:00 INFO  문서 예제를 준비했습니다.\n2026-09-10 09:00:01 INFO  저장소 연결: 사용하지 않음\n2026-09-10 09:00:02 INFO  편집 준비 완료\n```\n";
