import { codeExamples } from './code-examples';
import { createState, $getState, type LexicalNode } from 'lexical';

export const blockIdState = createState('gentaskBlockId', { parse: value => typeof value === 'string' ? value : '' });
export const blockId = (node: LexicalNode) => $getState(node, blockIdState);
export type BlockInfo = { key: string; id: string; text: string; type: string };
export type LocalComment = { id: string; blockId: string; text: string; quote: string };
export const sample = `# Requirements · 문서 편집과 검토

> 플레이그라운드용 요구사항 예제입니다. 운영 Gentask의 확정 요구사항이나 현재 구현 완료 목록이 아닙니다.

## 소개

사용자는 Gentask에서 요구사항과 설계 문서를 작성하고, 문서의 특정 블록에 의견을 남깁니다. 에이전트는 사용자가 요청하면 해당 의견과 문서를 읽고 수정안을 제안합니다. 이 문서는 그 과정에서 사용자가 기대하는 동작을 정의합니다.

## 용어

- **문서 편집기**: 제목, 문단, 목록, 표, 이미지와 코드 블록을 작성하는 영역입니다.
- **블록**: 코멘트의 대상이 되는 문단, 목록, 표, 이미지 또는 코드 블록입니다.
- **문서 버전**: 발행 시점의 본문을 보존한 변경 불가능한 기록입니다.
- **코멘트**: 특정 문서 버전의 블록에 연결된 사용자 의견입니다.

## 요구사항 1 · 문서 작성

**사용자 스토리**

문서 작성자로서, 작성 화면에서 본문의 구조와 서식을 바로 확인하고 싶습니다. 이를 통해 미리보기 화면을 반복해서 열지 않고 내용을 다듬을 수 있습니다.

### 인수 조건 · 작성

1. **WHEN** 작성자가 제목 서식을 선택하면, **THE 문서 편집기 SHALL** 선택한 문단을 해당 단계의 제목으로 표시해야 합니다.
2. **WHEN** 작성자가 실행 취소를 요청하면, **THE 문서 편집기 SHALL** 가장 최근의 편집 변경을 되돌려야 합니다.
3. **WHILE** 읽기 전용 버전을 보고 있는 동안, **THE 문서 편집기 SHALL** 본문 변경을 허용하지 않아야 합니다.
4. **THE 문서 편집기 SHALL** 한국어 입력 중 조합이 완료되지 않은 글자를 중복 삽입하거나 유실하지 않아야 합니다.

## 요구사항 2 · 코드 구문 강조

**사용자 스토리**

기술 문서 작성자로서, 코드의 언어와 구문을 구분해서 보고 싶습니다. 이를 통해 예제의 조건, 함수와 데이터 구조를 쉽게 이해할 수 있습니다.

### 인수 조건 · 코드

1. **WHEN** 작성자가 지원하는 코드 언어를 선택하면, **THE 문서 편집기 SHALL** 코드 원문을 유지하면서 해당 언어의 구문 강조를 적용해야 합니다.
2. **WHEN** 작성자가 일반 텍스트를 선택하면, **THE 문서 편집기 SHALL** 해당 코드 블록의 구문 색상을 제거해야 합니다.
3. **WHEN** 사용자가 라이트 또는 다크 모드를 변경하면, **THE 문서 편집기 SHALL** 선택한 모드에 맞는 코드 색상으로 전환해야 합니다.
4. **IF** 언어 정의를 불러오지 못하면, **THEN THE 문서 편집기 SHALL** 코드 원문을 편집 가능한 일반 텍스트로 유지해야 합니다.

## 요구사항 3 · 블록 코멘트

**사용자 스토리**

검토자로서, 의견이 어떤 내용에 대한 것인지 문서 안에서 확인하고 싶습니다. 이를 통해 같은 문장이 반복되어도 검토 대상을 혼동하지 않을 수 있습니다.

### 인수 조건 · 코멘트

1. **WHEN** 사용자가 블록을 선택하고 코멘트를 등록하면, **THE 시스템 SHALL** 해당 문서 버전과 블록 식별자에 코멘트를 연결해야 합니다.
2. **WHEN** 사용자가 코멘트의 대상 보기 기능을 선택하면, **THE 문서 편집기 SHALL** 연결된 블록으로 이동해야 합니다.
3. **WHEN** 작성자가 블록을 이동하면, **THE 시스템 SHALL** 해당 블록의 기존 코멘트 연결을 유지해야 합니다.
4. **IF** 코멘트 대상 블록이 삭제되면, **THEN THE 시스템 SHALL** 대상을 찾을 수 없음을 표시하고 다른 블록에 코멘트를 임의로 연결하지 않아야 합니다.

## 요구사항 4 · 버전 발행과 에이전트 검토

**사용자 스토리**

문서 소유자로서, 검토 의견을 반영한 내용을 새 버전으로 발행하고 싶습니다. 이를 통해 이전 문서와 그때의 의견을 함께 보존할 수 있습니다.

### 인수 조건 · 버전

1. **WHEN** 작성자가 수정본 발행을 완료하면, **THE 시스템 SHALL** 이전 버전을 보존하고 수정본에 새로운 버전 번호를 부여해야 합니다.
2. **WHEN** 에이전트가 특정 버전의 코멘트를 요청하면, **THE 시스템 SHALL** 요청한 버전의 코멘트와 대상 블록 정보를 반환해야 합니다.
3. **IF** 수정의 기준 버전보다 최신 버전이 이미 발행되었다면, **THEN THE 시스템 SHALL** 기존 내용을 덮어쓰지 않고 버전 충돌을 알려야 합니다.

## 범위에서 제외하는 항목

이번 요구사항 예제는 실시간 공동 편집, 코멘트 답글, 승인 절차를 다루지 않습니다. 아래 코드는 구문 강조를 비교하기 위한 부록이며 구현 설계를 확정하지 않습니다.

` + codeExamples;
