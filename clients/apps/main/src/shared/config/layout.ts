
/**
 * 화면 치수를 한곳에 모은다. 화면마다 값을 새로 고르면 폭과 높이가 갈린다.
 */

/**
 * 본문 폭 세 단계.
 *
 * PageLayout은 머리글과 본문의 폭을 맞추고, PageContent는 스크롤 영역 안의 본문에만
 * 최대 너비를 적용한다. 스크롤 영역과 보조 패널은 이 폭으로 제한하지 않는다.
 */
export const WIDTH = {
  /** 영역을 채운다. 칸반처럼 가로로 흐르는 화면에만 쓴다. */
  full: undefined,
  /** 목록과 상세의 기본값. */
  wide: '60rem',
  /** 한 열로 읽는 개인 화면. */
  narrow: '40rem',
} as const;

/** 목록과 상세를 나란히 두는 패널의 폭. */
export const PANEL = {
  /** 목차나 항목 목록. */
  list: '16.25rem',
  /** 메타 정보. */
  meta: '21.25rem',
} as const;
