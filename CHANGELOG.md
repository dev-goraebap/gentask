# 변경 이력

형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) · 베이스라인(태그) 단위로 절을 만든다.
분류: Added(추가) / Changed(변경) / Fixed(수정) / Removed(제거)

## [미출시]

### Added

- 프로젝트 초기 스캐폴드: Angular 22 (`web/`), Spring Boot 4.1 + jOOQ + PostgreSQL (`server/`)
- 프로세스 문서 체계: AGENTS.md(법전), 계획, 요구사항 5종, 결정기록, 참고 자료
- web 코딩 표준: angular-eslint 22(flat config) + eslint-config-prettier 연결, `lint`/`format:check` 스크립트 (#1)
- server 코딩 표준: Spotless(palantir-java-format) + Checkstyle 13.8 + SpotBugs, `check` 태스크 연결 (#2, 결정-0004)
- CI 게이트: PR·main push 시 web(린트+포맷+빌드+테스트), server(빌드+린트) 검증 (#4)
- 에이전트 자산 체계: 검토 페르소나 4종(qa-auditor, release-auditor, doc-sync, security-reviewer)을 개방 표준 위치(`.agents/skills/`)에 스킬로 제공, Claude용 동기화 스크립트 포함 (#3, 결정-0006)
- 부트스트랩 스크립트: 파생 프로젝트 초기화(정체성 교체, 문서 리셋, 기능 선택) 자동화 (#5)
- 라인엔딩 정책(`.gitattributes`): 텍스트는 LF 통일(bat만 CRLF) — 로컬/CI 포맷 판정 일치 (#12)
- 설계 문서 3종 — 인프라 아키텍처 1 + 애플리케이션 아키텍처 2(웹·서버) — 과 결정기록 4건(서버 모듈 구조, 프론트 FSD 채택, DTO record 허용, 조회 경로 설계) (#14, #16)
- 웹 렌더링·로딩 규칙: URL 접두사 기반 하이브리드 렌더링, 리졸버 + 임계 시간 오버레이, 에러 유형별 분기, 빈 상태 3케이스 (결정-0011·0012)
- server 기반 배선: 패키지 루트 `dev.goraebap.devkit`, Flyway, jOOQ 코드 생성(DDLDatabase), Testcontainers 통합 테스트, ArchUnit 7규칙, 스키마 헬스 인디케이터 (#20, 결정-0013)
- 인증 세션 방식 결정 및 AUTH-*·MAIL-* 요구사항 확정 (#22, 결정-0014)
- 이메일 소유 증명 방식 결정 — 제공자 이메일을 신뢰하지 않고 자체 OTP로 증명, 소유 증명 전에는 계정을 만들지 않는다 (결정-0015)
- 메일 발송 인프라 결정 — Gmail SMTP 어댑터 + 비동기 발송, 트랜잭션 커밋 이후 트리거, 재발송으로 복구 (결정-0016)
- 신규 요구사항 — AUTH-07(비밀번호 재설정, 전 세션 무효화 포함), AUTH-08(이메일 복구), PROF-05(이메일 변경). 로그인 불가 상태의 복구 흐름과 이메일 변경이 요구사항에 없던 공백을 채운다
- 서버.md §1.6 이메일 소유 증명 계약 — 대기 레코드 식별자 기반 검증, 2단계 소셜 로그인, 세션 미승격 규약
- 디자인 시스템 결정기록 3건 — 토큰 아키텍처(결정-0017, DTCG 빌드 미도입 근거 포함), 컴포넌트 워크벤치(결정-0018), 컴포넌트 동작 조달(결정-0019) (#26)
- 1차 UI 컴포넌트 8종(`web/src/shared/ui`) — Button · Input · Field · OtpInput · Alert · Card · Link · Spinner. 네이티브 요소 위에 속성 선택자로 얹어 폼 연동·의미를 그대로 쓴다. Field가 `label[for]`↔`id`·`aria-describedby`·`aria-invalid` 배선을 담당하고, OtpInput은 자리마다 입력을 두지 않고 투명한 입력 하나로 붙여넣기·자동완성·스크린리더를 네이티브로 얻는다 (#26)
- `docs/설계/디자인시스템.md` — 톤 & 매너, 색 역할 체계, 타이포그래피, 형태, 깊이, 모션, 컴포넌트 규칙, Do/Don't. 출처 팩 문서 없이도 판단할 수 있게 자족적으로 작성했다 (#26)
- 경로 별칭 `@/shared/*`·`@/app/*` (웹.md §4). `baseUrl`은 TypeScript 7에서 제거되므로 쓰지 않는다 (#26)
- 라이트·다크 양쪽 지원 — 색상은 `light-dark()`로 두 값을 한 줄에 선언해 모드별 블록이 어긋날 여지를 없앴다. 기본은 OS 선호를 따르고 `[data-theme]`으로 덮으며, 하위 트리에도 걸 수 있어 밝은 페이지 안의 어두운 영역이 공짜로 얻어진다. 라이트는 그림자가, 다크는 표면 색이 층위를 만든다 (#26)
- 팔레트 대비 검증 테스트 — 실제 CSS를 파싱해 WCAG 대비를 계산한다(33건). 본문·보조 텍스트는 AA 4.5:1, 비활성·경계선·채움은 UI 3:1. 팔레트 값이나 매핑이 바뀌면 그 자리에서 실패한다 (#26)
- Pretendard 자체 호스팅 — 변수 폰트 + 동적 서브셋(92개 유니코드 범위)으로 쓰인 글자 범위만 내려받는다. 한글·라틴을 한 서체로 덮어 섞인 문장에서 굵기·리듬이 어긋나지 않는다. OFL-1.1 (#26)
- 디자인 토큰 3계층 — 프리미티브(`app/styles/themes/halo.css`) → 시맨틱(`app/styles/tokens.css`, Tailwind 4 `@theme`) → 컴포넌트. 컴포넌트는 시맨틱만 읽으므로 테마 교체가 파일 한 줄 교체다. 표면 3단·전경 3단·경계선·액센트·의도 4종(success·warning·info·danger)·타입 스케일 9종·반경 5종·깊이 3종. 토큰 참조 화면을 Storybook 스토리로 제공한다 (#26)
- 전역 기반 스타일 — `scrollbar-gutter: stable`(스크롤바 등장으로 인한 레이아웃 시프트 방지), `:focus-visible` 링, `prefers-reduced-motion` 대응 (#26)
- 컴포넌트 워크벤치 Storybook — `@storybook/angular-vite`. Angular 22 zoneless에서 dev·빌드 모두 동작함을 시그널 갱신으로 검증했다(zone.js 미설치 유지). `format:check`가 `.storybook`도 검사한다 (#26)
- 인증 스키마 4테이블 — `users`·`sessions`·`accounts`·`verifications` (`V2__auth_schema.sql`). 결정-0015의 불변식을 DB 제약으로 강제한다: `email_verified_at` NOT NULL(미검증 사용자를 표현할 수 없다), `verifications`에 유일성 제약 없음(대기 시도가 이메일을 선점하지 못한다), `(provider, provider_account_id)` 유니크 (#24, AUTH-06)
- `docs/설계/데이터베이스.md` — 불변식↔제약 대응 표, 대기 레코드 수명주기, jOOQ DDLDatabase(인메모리 H2 경유)가 스키마에 거는 제약과 `[jooq ignore]` 우회 (#24)
- `AuthSchemaTest` — 스키마가 불변식을 실제로 막는지 검증하는 통합 테스트 9건. "제약의 부재"(대기 레코드 이메일 중복 허용)도 테스트로 고정했다 (#24)
- 결정기록 보충 자료(`docs/참고/결정-0014-보충-세션-방식.md`) — 결정-0014의 선택 배경·조회 비용 정량 비교·렌더링 모드별 영향·표준 현황(IETF BCP 초안·OWASP·RFC 9068/7662). 결정의 정본은 결정-0014이며 이 문서는 결정을 바꾸지 않는다. 보충 자료의 명명은 `결정-NNNN-보충-<주제>.md`를 따른다
- 이메일/비밀번호 인증 (AUTH-01) — OTP 발급(`POST /api/v1/email-verifications`)·가입 완료(`POST /api/v1/users`)·로그인(`POST /api/v1/sessions`)·현재 세션 조회·로그아웃. OTP 통과 전에는 `user` 행이 생기지 않고, 검증은 대기 레코드 식별자로만 조회하며, 가입·로그인 모두 세션 토큰을 새로 발급한다 (#28, 결정-0015)
- 세션 인증 기반 (AUTH-06 일부) — DB 세션 + 이중 전달(HttpOnly 쿠키·Bearer), HMAC-SHA256 토큰 저장, 슬라이딩 만료(연장 간격 제한으로 요청당 쓰기 방지), 로그아웃 즉시 무효화. 쿠키 경로 CSRF 방어는 `SameSite=Lax` + Origin 검증 필터, 인가는 기본 거부(deny-by-default) (#28, 결정-0014)
- 시도 제한 3종 — OTP 발급(IP 5회/10분·이메일 30회/시간, 비대칭 방향은 결정-0015 §결정 3), OTP 확인(IP 20회/10분), 로그인(IP 20회·계정 10회/10분). 발급에서 IP가 거부되면 이메일 카운터를 소비하지 않는다(남의 쿼터를 소진시켜 정상 가입·재발송을 막는 경로 차단). 로그인 제한은 bcrypt 계산 이전에 걸어 해시 비용 자체가 DoS 수단이 되지 않게 한다. 인메모리 고정 윈도우 구현이며 포트 뒤에 있어 다중 인스턴스 배포 시 공유 저장소 구현으로 교체한다 (#28)
- OTP 검증의 동시성 방어 — 대기 레코드를 `for update`로 잠그고 읽는다. 잠금이 없으면 동시 요청이 서로의 시도 횟수 증가를 덮어써(lost update) 5회 제한이 무력화되고, 같은 레코드에 코드를 병렬 대입해 10⁶ 공간을 탐색할 수 있다 (#28)
- 세션 절대 수명 상한(90일) — 슬라이딩 연장이 발급 시점 기준 상한을 넘지 못한다. 상한이 없으면 계속 쓰이는 세션이 영구 유효해져 탈취된 토큰이 만료로 회수되지 않는다 (#28)
- 만료 행 정리 스케줄러 — 매일 03:15에 만료된 세션·대기 레코드를 삭제한다(설계/데이터베이스.md §5). 정리가 밀려도 정확성에는 영향이 없다 — 만료 판정은 조회 시점에 한다 (#28)
- `V3__verification_target_email_raw.sql` — 대기 레코드에 이메일 원문을 보존한다. 정규화된 값만 저장하면 가입 완료 시 `users.email`에도 정규화된 값이 들어가 "원문을 저장하고 유일성은 정규화된 값에 건다"(결정-0015 §결정 5)가 지켜지지 않는다 (#28)
- `mail` 모듈 (MAIL-01) — 발송 포트(`MailSender`) + Gmail SMTP 어댑터. 비동기(전용 executor)·트랜잭션 커밋 후 트리거·짧은 재시도 후 실패 로깅(유스케이스 비중단)·수신자/제목 개행 차단(헤더 인젝션 방지)·자격증명 환경변수 주입 (#28, 결정-0016)
- `common` 모듈 — RFC 7807 전역 예외 처리(`code`·`traceId` 필드, 검증 실패 시 필드 이름만 노출), `X-Trace-Id` 요청 추적 필터, `BusinessException`/`ErrorCode` 계약 (#28, 설계/서버.md §1.3·§7)
- 전역 `Clock` 빈 — 시각은 DB 기본값이 아니라 주입된 Clock으로 채운다. 도메인·통합 테스트가 시각을 통제한다 (#28)
- 메일 발송 파이프라인 테스트 — 롤백 시 미발송·커밋 후 발송·전용 executor(`mail-` 스레드)·발송 실패의 호출자 비전파를 검증한다. 다른 테스트는 `MailSender`를 통째로 가짜로 바꾸므로 이 경로를 지나가지 않았다 — `TransactionAwareMailSender`를 지워도 아무 테스트도 실패하지 않던 공백을 메운다 (#28)
- 비밀 저장 형식 검증 — 가입 후 DB의 `password_hash`가 bcrypt 형식(`$2`)이고 원문을 포함하지 않는지, 세션 토큰·OTP가 HMAC hex 64자로 저장되며 원문과 다른지 직접 단언한다 (#28)
- OTP 동시성 회귀 테스트 — 같은 대기 레코드에 12개 스레드가 오답을 던져도 시도 횟수가 정확히 5에서 멈추는지, 정답을 동시에 던져도 계정이 하나만 생기는지 검증한다. 행 잠금을 지우면 이 테스트가 실패한다(확인함) — 치명 취약점이 조용히 되살아나는 것을 막는 장치다 (#28)
- 인증 테스트 60건 — 도메인 불변식(OTP 만료·5회 폐기·1회용, 슬라이딩 만료와 절대 상한, credential 불변식), 서비스 단위(가짜 저장소, 실패 사유 비구분·이메일 비예약·제한이 해시보다 먼저 걸림), 통합(가입→로그인→로그아웃 전체 흐름, Bearer 경로, Origin 검증, 저장소 왕복). OTP 코드는 항상 발송된 메일에서 관찰한다 (#28)
- 배포 전제 설정 명시 — `server.forward-headers-strategy=NATIVE` + `server.tomcat.remoteip.internal-proxies`(신뢰 프록시 대역), `auth.allowed-origins`, actuator health 상세를 ADMIN으로 제한. `FRAMEWORK`가 아니라 `NATIVE`인 이유는 Tomcat이 `X-Forwarded-For`를 오른쪽에서 왼쪽으로 훑으며 신뢰 프록시를 건너뛰기 때문이다 — Spring 필터는 첫 항목을 채택하는데 nginx 표준 레시피가 클라이언트가 보낸 값을 앞에 두고 덧붙이므로, 헤더 한 줄로 IP 기반 제한을 전부 우회할 수 있다 (#28)
- 소셜 로그인 2종 (AUTH-02·03) — 구글·카카오. Spring Security OAuth2 Client가 `state`·PKCE·리다이렉트 검증을 담당한다. 최초 로그인은 **2단계**다: 제공자 인증 → 이메일 입력 + OTP → `user`·`account` 생성. 제공자는 "이 사람이 그 계정 주인이다"만 증명하고 이메일 소유는 우리 OTP가 증명한다(결정-0015) (#34)
- 제공자 인증과 이메일 증명 사이의 중간 상태를 **서명된 짧은 표**로 잇는다 — 이 시점에는 이메일을 모르니 대기 레코드를 만들 수 없고, 이메일 없이 만들 수 있게 스키마를 열면 "가입 대기는 이메일을 향한다"는 성질이 흐려진다. 표는 `HttpOnly` 쿠키로만 전달하며 10분이면 만료되고 한 번 쓰면 지워진다. **"훔쳐도 OTP를 통과하지 못하니 안전하다"는 초기 설명은 틀렸다** — 훔친 사람은 자기 이메일로 통과하면 되기 때문이고, 그래서 표는 애초에 훔치기 어려워야 한다(아래 Fixed 참조) (#34)
- 이메일 키 통합 (AUTH-05) — 그 이메일에 이미 계정이 있으면 **코드를 보내지 않고** 연동을 안내한다. 보내면 공격자가 피해자 이메일로 소셜 계정을 만들어 두고 코드를 가로채려 시도할 여지가 생긴다. 안내 문구는 어느 수단으로 가입했는지 알려주지 않는다 — 알려주면 그것만으로 정보가 샌다 (#34)
- 제공자 토큰을 **저장하지 않는다** — 원래 인수조건은 `account`에 보관하는 것이었으나, 이메일 scope조차 요구하지 않아 제공자 API를 호출할 계획이 없다. 쓰지 않을 자격증명을 들고 있으면 우리 DB 유출이 남의 서비스 접근으로 번진다. 칸도 함께 없앴다 (아래 Removed 참조, 결정-0022) (#34)
- 콜백 경로를 `/auth/callback/{제공자}`로 둔다 — 제공자 콘솔에 등록된 주소가 프론트(4200)이므로 개발 중에는 dev-server 프록시가, 프로덕션에서는 같은 오리진이 이 경로를 서버로 넘긴다(설계/인프라.md §2) (#34)
- 소셜 로그인 테스트 12건 — 표 위조·만료 거부, 아는 신원 즉시 로그인, 처음 보는 신원의 2단계, 기존 계정이면 코드 미발송(AUTH-05), 로컬 가입자 이메일로는 소셜 계정이 붙지 않음, 용도가 다른 대기 레코드 거부 (#34)
- 소셜 로그인 통합 테스트 7건 (`SocialLoginFlowIntegrationTest`) — 성공 핸들러와 컨트롤러는 **테스트가 한 줄도 닿지 않던 파일**이었고 보안 검토가 찾은 F1·F3·F4가 전부 거기 있었다. 표가 URL이 아닌 쿠키로 가는지, 쓰고 나면 지워지는지, 표 없이는 이메일 단계를 지날 수 없는지, 미지의 제공자가 에러 화면으로 가는지, 같은 신원으로 두 번 완료할 수 없는지를 고정한다. 신원 재확인을 지우면 마지막 테스트가 실패한다(확인함) (#34)
- 비밀번호 재설정 (AUTH-07) — `POST /api/v1/password-resets`(코드 발급) · `/confirm`(새 비밀번호 설정). 성공 시 **그 사용자의 모든 세션이 사라진다**(요청한 세션 포함) — 비밀번호를 바꾸는 시점에 계정에 붙어 있던 접근을 전부 끊는 것이 목적이다. 대기 중인 이메일 변경도 함께 취소한다: 공격자가 이메일 변경을 걸어두고 피해자가 비밀번호를 재설정한 뒤 그 변경을 완료해 계정을 가져가는 경로(pre-hijacking 변종 4)를 닫는다 (#31)
- 이메일 계정 복구 (AUTH-08) — `POST /api/v1/account-recoveries` · `/confirm`. 비밀번호 없이 이메일 코드만으로 로그인하고 세션을 새로 발급한다. 응답의 `shouldSetPassword`로 인증 수단 추가를 안내한다 (#31)
- 기기 관리 (AUTH-06 완료) — `GET /api/v1/sessions`(로그인된 기기 목록, 현재 세션 표시) · `DELETE /api/v1/sessions/{id}`(개별 로그아웃). 삭제는 소유자 조건을 SQL에 함께 넣어 남의 세션을 지우는 경로를 애플리케이션 분기가 아니라 한 문장으로 닫았고, 없는 세션과 남의 세션을 구분해 응답하지 않는다 (#31)
- 복구 흐름의 계정 존재 비노출 — 계정이 없거나 비밀번호 없는 소셜 전용 계정이어도 같은 형태로 응답하고, 다섯 종류의 메일이 **같은 제목**을 쓴다. 코드를 보내지 않는 경우에도 **아무도 맞힐 수 없는 미끼 대기 레코드**를 남긴다: 남기지 않으면 코드를 다섯 번 틀려보는 것만으로 "시도 횟수 초과"가 나오는 쪽이 실재 계정임이 드러난다(보안 검토 지적). 미끼를 제거하면 회귀 테스트가 실패한다(확인함) (#31)
- 복구 로그인 알림 메일 (AUTH-08) — 이 경로는 비밀번호 없이 세션을 내주면서 기존 세션을 끊지 않으므로, 메일함 접근을 얻은 사람이 조용히 들어올 수 있다. 알림이 본인이 알아차릴 유일한 신호다 (#31)
- 재설정 시 대기 중인 **모든** 코드 취소 — 이메일 변경뿐 아니라 복구·재설정 코드도 지운다. 살아 있는 복구 코드가 남으면 공격자가 재설정 직후 그 코드로 비밀번호 없이 되들어올 수 있다 (#31)
- 복구 테스트 23건 — 용도 교차 거부(재설정↔복구 양방향), 전 세션 무효화, 대기 중 이메일 변경 취소, 남의 세션 삭제 차단, 코드 1회용·만료. 통합 테스트는 코드를 항상 발송된 메일에서 읽는다 (#31)
- 남용 방지 계층 전략 결정 초안 — 엣지(Cloudflare 요청률) · 애플리케이션(이메일·계정 단위) · 사람 증명(Turnstile) 3층으로 나눈다. 엣지는 요청 본문을 열지 않아 계정 단위 방어를 못 하고 앱만 두면 모든 요청이 서버에 도달한 뒤 거부되므로, 둘은 서로를 대체하지 못한다. 한도 도달 시 차단 대신 사람 증명을 요구해 정상 사용자 잠김과 표적 계정 잠금을 함께 없앤다. 상태는 `제안` — 규칙 수치·발동 임계는 Cloudflare 구성 확인 후 확정 (결정-0021, #30)

### Changed

- **디자인 시스템을 Krill로 교체** (결정-0024, #38) — 토큰의 정본이 `krill.css` 한 장이 됐다. 3계층(프리미티브 `halo.css` → 시맨틱 `tokens.css` → 컴포넌트)을 버렸고, 대신 **`krill.css`는 손으로 고치지 않고 통째로 갈아끼운다**는 규율을 얻었다. 고쳐야 할 값은 `styles.css`의 오버라이드 블록에 모은다 — 벗어난 지점이 우리 파일에만 있어야 업스트림 갱신 시 무엇을 지울지 안다. Krill은 여러 프로젝트에 공통으로 쓰려고 만든 시스템이라 키트가 그것을 쓰면 파생 프로젝트도 같은 기반을 물려받는다
- 다크 전환을 `light-dark()` + `[data-theme]`에서 **`.dark` 클래스**로 변경 (결정-0024) — Krill 토큰이 그 방식이다. `light-dark()`가 공짜로 주던 OS 선호 추종이 사라지므로 `index.html`에 **첫 페인트 전에 실행되는 인라인 스크립트**를 넣었다. 앱 코드에서 붙이면 라이트로 한 번 그렸다가 어두워지는 깜빡임이 생긴다. `localStorage`의 `theme`이 OS 선호를 이기며, 토글 UI가 생기면 그 키에 쓰기만 하면 맞물린다. **감수하는 것**: 하위 트리만 다른 모드로 두는 것이 더 이상 공짜가 아니다
- 깊이 표현을 **그림자에서 선으로** 변경 (결정-0024) — 같은 평면은 1px 헤어라인으로 가르고 그림자는 실제로 떠 있는 것(모달·팝오버·드롭다운)에만 쓴다. 정적 카드에 그림자를 얹으면 "떠 있음"이라는 신호가 값싸져 진짜 떠 있는 것과 구분되지 않는다
- 컴포넌트 8종의 내부 클래스를 Krill 카탈로그 조합으로 교체 (#38) — **결정-0018(Storybook)·결정-0019(속성 선택자·접근성 배선)는 대체하지 않는다.** 컴포넌트 계약은 그대로이고 표현만 바뀌었다. 버튼의 `quiet` 변형은 Krill 어휘를 따라 `tertiary`로 이름을 바꿨다(호출부가 아직 없어 지금이 바꿀 수 있는 시점)
- 대비 검증 테스트를 Krill 계약에 맞게 재작성 — 33건 → **58건**. 색 파서를 OKLCH → OKLab → 선형 sRGB → 감마 sRGB로 다시 썼고, 반투명 값(다크의 보더들)은 뒤에 깔린 면 위에 합성한 뒤 계산한다. **`border-control`의 3:1을 새로 강제**하고, 장식선(`border`·`border-strong`)은 Krill이 WCAG 1.4.11을 의도적으로 면제하므로 검증하지 않되 근거를 파일에 남겼다 — 느슨해진 것이 아니라 더 정교하게 나눈 것이다. `krill.css`뿐 아니라 `styles.css`의 오버라이드까지 읽어 **앱이 실제로 싣는 값**을 검증한다
- 폰트는 현행 유지 (결정-0024) — Krill 문서는 CDN `@import`를 보여주지만 채택하지 않았다. Pretendard 자체 호스팅 + 동적 서브셋이 외부 의존이 없고 전송량도 적다. `krill.css`의 `--font-sans`가 `'Pretendard Variable'`을 첫 후보로 둬 그대로 맞물린다

- 서버.md §1에 인증 계약 절 추가, 모듈 트리의 근거 없는 `token/` 세그먼트를 `mail/`로 정정 (#22)
- AUTH-01 이메일 검증 정책을 "가입 후 검증"에서 "OTP 선행"으로 변경 — 미검증 계정을 남기지 않는 것이 pre-hijacking 계열 공격의 전제를 없애는 방법이기 때문 (결정-0015)
- AUTH-05 통합 키를 "제공자가 준 이메일"에서 "우리가 검증한 이메일"로 변경. 연동 규칙은 기존 방침(기존 계정 발견 시 로그인 후 연동) 유지 (결정-0015)
- AUTH-02~04에서 이메일 scope 요구를 제거하고 소셜 최초 로그인을 2단계로 변경. 제공자별 이메일 검증 신호 유무(네이버는 제공하지 않음)에 의존하지 않게 됐다 (결정-0015)
- auth 모듈 트리에 `recovery/` 피쳐 추가 (AUTH-07·08)
- 계획.md M1·M2 범위에 신규 요구사항 반영
- 웹.md §4에 Angular 제약 2건 추가 — 템플릿에서 참조되는 식별자는 ASCII여야 한다(Angular 템플릿 파서가 한글 식별자를 거부하며 TypeScript는 허용하므로 컴파일 단계에서야 드러난다), 투영된 콘텐츠는 DI로 부모를 찾지 못하므로 `contentChild`로 협력한다 (#26)
- ESLint 선택자 규칙에 `ui` 접두사와 attribute 타입 허용, 표기는 kebab-case로 통일 — `<button ui-button>`·`<input ui-input>` (#26)
- 서버.md §9 테스트 규약 보강 — 요구사항 ID는 `@DisplayName`에 둔다(메서드명의 `AUTH_06`은 하이픈 검색에 걸리지 않아 추적성이 끊긴다). 한국어 완화는 메서드명에만 적용되며 지역변수·파라미터는 프로덕션과 같은 규칙을 따른다 (#24)

- Lombok 사용 범위를 층별로 명문화하고 기존 코드를 전환 (결정-0023) — Spring 빈은 `@RequiredArgsConstructor`, 도메인 객체는 손으로 쓴 생성자(불변식을 지키는 자리이므로), DTO는 record, 로거는 `@Slf4j`. `@Getter`·`@Data`·`@Builder`는 쓰지 않는다. 그동안 방침이 없어 `@Slf4j`만 8곳에 쓰이고 생성자 주입 28곳은 전부 손으로 쓰인 상태였다. 빈 26곳을 전환해 상용구 150줄이 사라졌고, 생성자가 값을 **계산하는** 2곳(`SessionService`의 타이밍 균등화 해시, `HmacTokenHasher`의 키 생성)은 이유를 주석으로 남기고 손에 뒀다 (#34, #35)
- AGENTS.md 문서 원칙에 "린터 설정으로 표현할 수 없는 코딩 표준은 결정기록에 둔다" 추가 — "린터가 곧 표준"만 두면 린터로 표현 못 하는 표준이 적을 곳을 잃고 증발한다 (결정-0023)
- 프로세스 기준을 SP인증 2등급 테일러링에서 자체 경량 프로세스 선언으로 전환 — 심사 계획이 없는데 인증 기준을 명분으로 유지하면 과잉 산출물 제안의 근거가 되기 때문. 실질 장치(요구사항 상태 관리·결정기록·CI 게이트·검토 페르소나·베이스라인 감사)는 전부 유지 (결정-0020)

### Fixed

- **Krill 다크 모드의 `border-control`이 WCAG 1.4.11(3:1)에 미달**하던 것을 `styles.css`에서 덮어썼다 — 배경 위 2.67:1, 표면 위 2.71:1이었고 알파를 0.30 → 0.34로 올려 3.09·3.12가 됐다. 라이트는 3.02·3.24로 통과하므로 다크만 놓친 것으로 보인다. 폼 컨트롤 테두리는 "여기가 입력란"임을 알리는 유일한 시각 정보라 면제할 수 없다. 업스트림(dev-goraebap/krill)에 반영되면 오버라이드를 지운다. 재현 수치와 검증 절차는 `docs/참고/krill-대비-보고.txt` (#38)
- **제공자 신원 선점(계정 pre-hijacking) 차단** — 소셜 로그인의 중간 표를 URL 쿼리에서 `HttpOnly`·`SameSite=Lax` 쿠키(`Path=/api/v1/social-logins`, 10분)로 옮기고 한 번 쓰면 지운다. 쿼리에 실린 표는 액세스 로그·`Location` 헤더·브라우저 히스토리·`Referer`에 남는데, 그것을 주운 사람은 피해자 이메일이 아니라 **자기 이메일**로 소유 증명을 통과하면 된다 — 그러면 "피해자의 구글 신원 + 공격자의 이메일"인 계정이 생기고, 이후 피해자가 구글 로그인을 누를 때마다 공격자 계정으로 들어가며 유니크 제약 때문에 피해자는 자기 신원을 영영 못 붙인다. 결정-0015가 없애려던 pre-hijacking이 "표"라는 새 부품으로 되살아난 형태였다 (#34, 보안 검토 F1)
- **PKCE를 실제로 켰다** — 인수조건과 CHANGELOG는 적용된다고 적혀 있었으나 꺼져 있었다. 기본 리졸버는 클라이언트 인증이 `none`이거나 `ClientSettings.requireProofKey`가 참일 때만 PKCE를 붙이는데 구글·카카오는 기밀 클라이언트이고, Spring Boot 4.1 프로퍼티에는 `client-settings` 바인딩이 없어 설정으로는 켤 수조차 없다. `OAuth2AuthorizationRequestResolver` 빈으로 켠다. **있다고 적힌 방어가 없는 것은 없는 것보다 나쁘다** (#34, 보안 검토 F2)
- **인증 없는 세션 무제한 생성 차단** — `oauth2Login`이 인가 요청의 `state`·`code_verifier`를 보관하려고 요청마다 서블릿 세션을 만드는데, 시작 경로는 로그인 전이라 열려 있어 반복 호출만으로 힙을 고갈시킬 수 있었다. IP 단위 제한 필터 + 세션 수명 5분 + 세션 쿠키 `HttpOnly`/`SameSite`/`Secure`로 막는다. `STATELESS` 선언이 서블릿 세션 부재를 뜻하지 않는다는 점을 설정과 주석에 명시했다 (#34, 보안 검토 F3)
- **같은 제공자 신원으로 두 번 가입 완료 시 500 대신 업무 오류** — `completeSignup`에 신원 재확인을 넣었다. 없을 때는 `user` 삽입까지 성공한 뒤 `accounts` 유니크 제약에 걸려 500이 났다 — 비즈니스 오류를 DB 제약 위반으로 처리하는 셈이라 로그가 오염되고 원인이 가려진다. 동시 요청이 재확인을 함께 통과하는 경우를 위해 `DataIntegrityViolationException`도 409로 변환한다 (#34, 보안 검토 F4)
- **등록되지 않은 제공자로 인증이 들어오면 에러 화면으로 보낸다** — `AuthProvider.fromValue`가 서블릿 필터 안에서 던져 `@RestControllerAdvice` 밖으로 새고 컨테이너 기본 에러 페이지가 나오던 경로. 파생 프로젝트가 제공자 등록만 추가하고 enum 상수를 빠뜨리면 바로 걸린다 (#34, 보안 검토 F7)
- server 테스트가 DB 설정 부재로 실패하던 문제 — Testcontainers 도입으로 해결하고 CI 게이트에 편입 (#7)

### Removed

- 토큰 3계층의 두 파일 — `web/src/app/styles/themes/halo.css`(프리미티브)와 `tokens.css`(시맨틱). Krill이 한 장으로 대신한다 (#38, 결정-0024). Halo 팔레트(Uiverse Design 구매 라이선스) 참조도 함께 사라졌다
- `accounts`의 제공자 토큰 3컬럼(`access_token`·`refresh_token`·`token_expires_at`)과 `SocialIdentity`·`Account`의 대응 필드 — 그 토큰은 사용자를 대신해 제공자 API를 부를 때만 쓸모가 있는데 이 키트에는 그 용도가 없어, `null` 세 개가 핸들러→신원 타입→도메인→저장소를 지나 빈 칸에 도달하고 있었다. 빈 칸은 언젠가 평문으로 채워지고 그러면 우리 DB 유출이 남의 서비스 침해로 번진다. **없는 칸은 잘못 채워질 수 없다.** 배포 이력이 없으므로 `V2__auth_schema.sql`을 직접 고쳤다 (#34, 결정-0022)
- 인증 세션 방식 참고 문서 중복분 2건(`세션-vs-JWT-논쟁-대응.md`, `인증-세션-방식-기술검토서.md`) — 같은 내용의 초기 판이며 `결정-0014-보충-세션-방식.md`로 통합
- 인증 기준 근거 자료 3건(`docs/참고/`의 기준 전문 txt·pdf, 커버리지 맵) — 프로세스 기준 자체화에 따라 참조 대상이 사라짐 (결정-0020)
