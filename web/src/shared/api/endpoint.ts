/**
 * API 경로 접두사 (설계/서버.md §1.2 — 모든 API는 `/api/v1` 아래에 있다).
 *
 * 절대 URL이 아니라 경로만 두는 것은 **same-origin 배포가 전제**이기 때문이다
 * (설계/인프라.md §2). 개발 중에는 dev-server 프록시가 같은 오리진을 흉내낸다
 * (`web/proxy.conf.json`). 오리진이 갈라지는 배포가 필요해지면 CORS·쿠키 정책을
 * 결정기록으로 남기는 일이 먼저이고, 이 상수를 환경 설정으로 바꾸는 것은 그 다음이다.
 */
export const API_V1 = '/api/v1';
