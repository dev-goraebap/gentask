import type { Meta, StoryObj } from '@storybook/angular-vite';

import { ZonelessSpike } from './zoneless-spike';

const meta: Meta<ZonelessSpike> = {
  title: 'Spike/Zoneless',
  component: ZonelessSpike,
};

export default meta;

export const Default: StoryObj<ZonelessSpike> = {};
