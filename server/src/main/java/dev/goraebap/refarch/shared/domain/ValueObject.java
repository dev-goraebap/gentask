package dev.goraebap.refarch.shared.domain;

/**
 * 값 객체임을 표시한다.
 *
 * <p><b>검증은 정적 팩토리 {@code of(...)} 가 하고 정규 생성자는 이미 검증된 값을 받는다.</b>
 * 저장소가 재구성할 때 그 생성자를 직접 쓰며, 그 밖의 계층에서 쓰는 것을 ArchUnit 이 막는다.
 *
 * <p>경로를 둘로 두는 이유는 <b>애플리케이션 데이터베이스가 신뢰 경계 안</b>이기 때문이다.
 * 우리가 검증해서 넣은 값이므로 읽을 때 다시 검증할 이유가 없다. 객체의 사설 필드를 읽을 때
 * 검증하지 않는 것과 같은 논리다. ORM 을 쓰는 구성에서는 리플렉션이 생성자를 우회해 이것이
 * 자동으로 성립하지만, 손으로 매핑하는 구성에서는 경로를 직접 나눠야 한다.
 *
 * <p>다시 검증하면 <b>규칙을 강화한 날 옛 데이터를 읽지 못합니다.</b> 제목 상한을 줄이는 것만으로
 * 그보다 긴 기존 행이 로드에서 실패하며, 그것은 데이터가 잘못된 것이 아니라 규칙이 나중에
 * 바뀐 것이다.
 *
 * <p>표식만 갖고 메서드를 두지 않는다. 값 객체는 형태가 제각각이라 공통 계약을 억지로 만들면
 * 그것이 모든 값 객체를 제약한다. 이 인터페이스의 쓸모는 <b>ArchUnit 이 대상을 식별하는 것</b>
 * 하나다.
 */
public interface ValueObject {}
