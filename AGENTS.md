# AGENTS.md — 웹앱 개발키트

이 문서는 이 저장소에서 일하는 모든 에이전트(Claude, Codex, Gemini 등)가 가장 먼저 읽는 **법전**이다.
프로세스의 원본은 이 파일이며, 상세 내용은 `docs/`에 있다. 이 파일은 얇게 유지한다.

## 1. 정체성

- **이 저장소는 개발 키트(원본)이다.** 새 웹앱 프로젝트를 시작할 때 복제해서 쓰는 스타터 키트.
  <!-- 파생 프로젝트에서는 부트스트랩 시 이 절을 "이 저장소는 웹앱 개발키트 vX.Y에서 파생된 프로젝트이다"로 교체한다 -->
- 핵심 자산은 스택 코드가 아니라 **프로세스 체계 + 기능 명세 + 설계 결정**이다.
- 현재 스택: Angular 22 (`web/`) + Spring Boot 4.1 / Java 21 / jOOQ / PostgreSQL (`server/`)
- 프로세스 기준: SP인증(SW프로세스 품질인증) 2등급을 1인 + AI 에이전트 체제로 테일러링. 근거 자료는 `docs/참고/`.

## 2. 문서 지도

| 무엇을 할 때 | 읽을 것 | 갱신할 것 |
|---|---|---|
| 어떤 작업이든 시작 전 | 이 문서, `docs/계획.md` | — |
| 기능 구현 전 | `docs/요구사항/<기능>.md`, `docs/설계/` | 요구사항 상태(제안→확정) |
| 아키텍처·기술 결정 시 | `docs/결정기록/` | 새 결정기록 추가 (기존 것 수정 금지) |
| 기능 구현 후 | — | `CHANGELOG.md`, 요구사항 상태(확정→구현됨) |
| 마일스톤 종료 시 | `docs/계획.md` | `docs/회고/`, 계획 대비 어긋남은 계획.md 수정 |
| 베이스라인(태그) 전 | 형상 항목 전체 | `docs/감사/`에 형상감사 결과 |

- 요구사항 ID 컨벤션: `AUTH-01`, `FILE-02` 형식. 이슈·커밋·테스트 이름에서 이 ID를 참조해 추적성을 유지한다.
- 결정기록(ADR)은 불변이다. 결정이 바뀌면 새 문서를 쓰고 기존 문서를 "대체됨"으로 표시한다.

## 3. 명령어

| 작업 | web/ | server/ |
|---|---|---|
| 개발 서버 | `npm start` | `./gradlew bootRun` |
| 테스트 | `npm test` | `./gradlew test` |
| 빌드 | `npm run build` | `./gradlew build` |
| 린트 | `npm run lint` (품질) / `npm run format:check` (포맷) | (M0에서 구성 예정) |

## 4. 규칙

### 역할 분담

- **사람(devgo) = 승인자**: 범위·우선순위 결정, 계획 변경 승인, 결정기록 최종 결정, 인수 판정, 베이스라인 승인. 이 목록은 에이전트에게 위임하지 않는다.
- **에이전트 = 수행자**: 요구사항·설계 초안, 구현+테스트, QA 검토, 감사·회고 초안, 문서 정합성 검사.
- **CI = 측정자**: 빌드·테스트·린트 게이트, 커버리지·지표 수집. (M0에서 구성 예정)

### 완료 기준 (Definition of Done)

기능이 "완료"이려면 전부 충족해야 한다:

1. 요구사항 문서의 인수조건 충족
2. 테스트 존재 (테스트 이름에 요구사항 ID 참조)
3. 빌드·테스트·린트 통과
4. `CHANGELOG.md`에 변경 기재
5. 요구사항 문서 상태 갱신

### 형상 항목 (통제 대상 산출물)

소스코드(`web/`, `server/`) · DB 스키마/마이그레이션 · API 계약 · `docs/` 전체 · `CHANGELOG.md` · CI 설정 · 린터 설정 · 이 문서.
이 목록에 없는 것(빌드 산출물, 로컬 설정)은 통제 대상이 아니다.

### 베이스라인(태그) 정책

- 태그 = 베이스라인. 마일스톤 완료 시점에 사람이 승인하여 찍는다.
- 태그 전 형상감사 필수: 형상 항목이 전부 정합한지(문서 상태·CHANGELOG·테스트) 확인하고 결과를 `docs/감사/`에 남긴다.
- 명명 규칙: `v<major>.<minor>` (예: `v0.1`). 스택 메이저 업그레이드 시 major 증가.

### git 전략

- 기본 브랜치는 `main`. 항상 베이스라인(태그) 가능한 상태를 유지한다.
- **코드·설정 변경**: 이슈 등록 → 브랜치 `feat/<이슈번호>-<설명>` → PR → CI 게이트 → squash 머지. 머지는 사람이 승인한다.
- **문서만의 소규모 수정**(오타·문구): main 직접 커밋 허용.
- 커밋 메시지는 Conventional Commits를 따른다: `feat:` `fix:` `docs:` `chore:` `build:` `test:` `refactor:`. 요구사항 관련 작업은 본문에 ID(`AUTH-01` 등)를 참조한다.

### 결함 처리

결함 발견 → GitHub 이슈 등록(라벨 `bug`) → 수정 + 재발 방지 테스트 → 이슈 닫기. 베이스라인 이후 발견된 결함은 CHANGELOG의 Fixed에 기재한다.

### 문서 원칙

- **원본만 손으로 쓴다.** 사람의 결정이 담기는 문서(계획·요구사항·설계·결정기록)만 유지하고, 파생 가능한 것(추적 매트릭스, 진척 보고, 측정 대장)은 만들지 않는다. 필요하면 그 시점에 생성한다.
- 코딩 표준은 산문으로 쓰지 않는다. 린터 설정 파일이 곧 표준이다.

### 테일러링 선언 (SP인증 2등급 대비)

- 협력업체 관리(1.3): 해당 없음 — 외부 조달 없는 1인 프로젝트.
- 공수·비용 견적(1.1.4~5): 일정 약속이 없는 키트 특성상 마일스톤 순서와 상대 크기(S/M/L)로 갈음.
- 그 외 항목의 충족 방식은 `docs/참고/`의 커버리지 맵 참조.
